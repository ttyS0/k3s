apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: amber
  namespace: minecraft
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10G
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: amber
  namespace: minecraft
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: minecraft
      world: amber
  template:
    metadata:
      labels:
        app: minecraft
        world: amber
    spec:
      securityContext:
        runAsUser: 1000
        fsGroup: 1000
      nodeName: elrond
      containers:
        - name: amber
          image: ghcr.io/ttys0/spigot:1.18.1
          workingDir: "/minecraft/amber"
          ports:
            - containerPort: 25565
            - containerPort: 8123
            - containerPort: 25575
          volumeMounts:
            - name: amber
              mountPath: /minecraft/amber
          env:
            - name: JVM_OPTS
              value: "-Xms4G -Xmx4G -Djava.util.concurrent.ForkJoinPool.common.parallelism=1"
            - name: SPIGOT_DIR
              value: "/minecraft/amber"
            - name: SPIGOT_OPTS
              value: "nogui --noconsole"
      volumes:
        - name: amber
          persistentVolumeClaim:
            claimName: amber
---
apiVersion: v1
kind: Service
metadata:
  name: amber
  namespace: minecraft
  labels:
    app: minecraft
    world: amber
spec:
  ports:
    - name: server
      port: 25565
      targetPort: 25565
    - name: map
      port: 8123
      targetPort: 8123
    - name: rcon
      port: 25575
      targetPort: 25575
  selector:
    app: minecraft
    world: amber

